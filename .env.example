# Configuración del Puerto
PORT=3000

# Entorno de Desarrollo
NODE_ENV=development

# Base de Datos MongoDB
# Para desarrollo local:
MONGODB_URI=mongodb://localhost:27017/tienda_online

# Para MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/<database>?retryWrites=true&w=majority

# JWT - Clave Secreta para Tokens
# IMPORTANTE: Cambiar por una clave segura en producción
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_clave_secreta_muy_segura_cambiar_en_produccion

# ================================================================
# INSTRUCCIONES PARA CONFIGURACIÓN:
# ================================================================
# 1. Copiar este archivo como .env: cp .env.example .env
# 2. Modificar los valores según tu entorno
# 3. NUNCA subir el archivo .env al repositorio (está en .gitignore)
# 4. Para producción, generar un JWT_SECRET seguro y único
# 5. Configurar la URL de MongoDB según tu base de datos
# ================================================================